# Cau hinh test - H2 in-memory (MODE PostgreSQL), khong can PostgreSQL that
spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:order;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop