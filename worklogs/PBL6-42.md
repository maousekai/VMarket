# Worklog — PBL6-42: FR-AUTH-01 User Registration

- **Story / subtask:** PBL6-12 Auth Service — PBL6-42 (FR-AUTH-01)
- **Branch:** `PBL6-42-user-registration` (nhánh từ `dev` sau khi PBL6-41 merge — `ee46c53`)
- **Ngày:** 06/09/2026
- **Phạm vi:** endpoint đăng ký `POST /api/auth/register` — validate, hash BCrypt,
  check trùng, tạo user role BUYER, Swagger. **Không** làm: gửi email thật, đăng nhập/JWT.

---

## 1. Quyết định thiết kế (đã xác nhận trước khi code)

| # | Quyết định |
|---|---|
| Password | 8–32 ký tự, **≥1 chữ hoa + ≥1 số + ≥1 ký tự đặc biệt** (regex 1 dòng, message rõ) |
| Kích hoạt | **Thêm cột `email_verified` ngay** (migration V2) → user mới = `PENDING`. Gửi mail + endpoint xác nhận: PBL6-45 |
| Username | 3–50 ký tự `[a-zA-Z0-9._-]`, **unique phân biệt hoa/thường** (khớp constraint hiện có, không chuẩn hoá) |
| Default role | `BUYER` (RBAC matrix: chỉ Buyer mới đăng ký mở gian hàng) |
| Endpoint | `POST /api/auth/register`, body `{email, username, password}`, `201` trả `{id, email, username, status, roles, createdAt}`. **Chưa** trả JWT (đăng nhập = PBL6-43) |
| Email | Lưu **lowercase + trim** → tránh trùng do khác hoa/thường. Username cũng `trim()` cho đồng nhất |
| Idempotency-Key | Chưa áp dụng cho `/register` (không phải luồng tiền/đơn cần retry an toàn). Quyết định cuối: chờ nhóm chốt quy ước chung |
| User enumeration | Trả code riêng `EMAIL_ALREADY_EXISTS` / `USERNAME_ALREADY_EXISTS` — **chấp nhận** để UX rõ ràng; xem lại nếu audit bảo mật yêu cầu che |

## 2. File đã tạo / sửa

### Thêm mới
- `resources/db/migration/V2__add_users_email_verified.sql` — `ALTER TABLE users
  ADD COLUMN email_verified BOOLEAN NOT NULL DEFAULT false` (expand-contract).
- `dto/RegisterRequest.java` — record + Jakarta validation (`@Email`, `@Pattern`).
- `dto/RegisterResponse.java`, `dto/AccountStatus.java` (enum PENDING/ACTIVE),
  `dto/ErrorResponse.java` (body lỗi chuẩn `{ error: { code, message, details } }`).
- `exception/ApiException.java` — lỗi nghiệp vụ có `code` + `HttpStatus`.
- `exception/GlobalExceptionHandler.java` — `@RestControllerAdvice`: `ApiException`
  → status tương ứng; `MethodArgumentNotValidException` → 400 `VALIDATION_ERROR`
  + `details` từng trường; `HttpMessageNotReadableException` → 400 `MALFORMED_REQUEST`;
  `MethodArgumentTypeMismatchException` → 400; `NoResourceFoundException` → 404;
  `HttpRequestMethodNotSupportedException` → 405; `HttpMediaTypeNotSupportedException`
  → 415; fallback `Exception` → 500 `INTERNAL_ERROR` (log, không lộ stack).
- `service/RegistrationService.java` — check `existsByEmail/Username` → `ApiException`
  409; hash `passwordEncoder.encode`; `saveAndFlush` (bắt `DataIntegrityViolationException`
  cho race) → gán `UserRole(BUYER)`.
- `controller/AuthController.java` — `POST /api/auth/register`, `@Valid`, Swagger
  `@Operation`/`@ApiResponses` (201 kèm schema, 400/409/500).
- `src/test/java/.../RegistrationApiTest.java` — test tích hợp trên H2: happy path,
  hash, PENDING, trùng email/username, password yếu, username sai, JSON hỏng,
  email quá dài, username quá ngắn, sai method (405), sai media-type (415).
- `src/test/java/.../RegistrationServiceRaceTest.java` — mock thuần cho nhánh race:
  tên constraint `users_email_key` / `users_username_key` / lạ → `EMAIL_ALREADY_EXISTS`
  / `USERNAME_ALREADY_EXISTS` / `REGISTRATION_CONFLICT`.

### Sửa
- `entity/User.java` — thêm field `boolean emailVerified`.
- `services/auth-service/README.md` — bảng endpoint, ràng buộc đăng ký, roadmap.

## 3. Kiểm thử

- `./mvnw.cmd -pl auth-service -am test` — **BUILD SUCCESS**. `RegistrationApiTest`
  (tích hợp, H2) + `RegistrationServiceRaceTest` (mock) + `HealthControllerTest` +
  `AuthServiceApplicationTests`.
- Chưa chạy đường PostgreSQL thật cho V2 (session không có Docker) — migration V2
  đơn giản (`ADD COLUMN ... DEFAULT`), sẽ chạy khi app boot với Postgres. Việc chạy
  Flyway thật + `ddl-auto: validate` bằng Testcontainers để dành PBL6-47.

## 4. Rà soát code — 2 lượt

**Lượt 1 (7 điểm, xử lý 6):**

| # | Điểm | Xử lý |
|---|---|---|
| 1+4 | `duplicateConflict` truy vấn lại DB trong catch → trên PostgreSQL transaction đã abort → 500 thay vì 409 khi 2 request cùng lúc | Bỏ truy vấn DB, suy trường trùng từ tên constraint trong `org.hibernate.exception.ConstraintViolationException`; không rõ → 409 `REGISTRATION_CONFLICT` |
| 2 | Không handler cho `HttpMessageNotReadableException` → body JSON hỏng trả 500 | Thêm handler → 400 `MALFORMED_REQUEST` |
| 3 | `toLowerCase()` dùng locale mặc định | `toLowerCase(Locale.ROOT)` |
| 6 | V2 chiếm version mà header V1 "đặt chỗ" cho subtask sau | Cập nhật javadoc `User.java` + header `V2` (migration tiếp theo là **V3**); không sửa file V1 (tránh đổi checksum Flyway) |
| 7 | `roles` trong response hardcode | Lấy từ `defaultRole.getName().name()` |
| 5 | `POST /register` không nhận `Idempotency-Key` | **Để ngỏ** — cần hạ tầng dedup dùng chung, chưa service nào có. Chờ nhóm chốt quy ước. |

**Lượt 2 (review của reviewer thứ 2 trên PR #5):**

| # | Điểm | Xử lý |
|---|---|---|
| F2 | Fallback `Exception` biến lỗi client (sai method / media-type / path / kiểu param) thành 500 | Thêm 4 handler: 405 / 415 / 404 / 400 (type mismatch) |
| F3 | Nhánh race + `constraintName()` + migration V2 chưa có test chạm tới | Thêm `RegistrationServiceRaceTest` (mock, 3 ca); thêm ca biên validate + 405/415 vào `RegistrationApiTest`; Testcontainers → PBL6-47 |
| F5 | Email được trim, username thì không | `username().trim()` cho đồng nhất |
| F6 | User enumeration (2 code riêng) | Không đổi hành vi — ghi nhận là quyết định có chủ đích (bảng mục 1) |
| F7 | README tick `[x] FR-AUTH-01` trong khi phần "gửi email xác thực" của SRS chưa có ticket | Sửa cách tick + thêm mục TODO tạo backlog cho email activation |
| F8 | `@ApiResponses` thiếu 409 `REGISTRATION_CONFLICT`, 500, và schema cho 201 | Bổ sung đủ |
| F1 | Comment/worklog trích một file quy ước cục bộ không nằm trong repo → reviewer không tra được | Bỏ mọi tham chiếu, viết quy ước thẳng trong comment. Vài chỗ đã merge từ PBL6-41 sẽ dọn khi tạo branch kế tiếp |

## 5. TODO chuyển tiếp

- **Migration Flyway tiếp theo phải là V3** (V2 đã dùng cho `email_verified`).
- **Idempotency-Key cho `/register`** — chờ nhóm chốt quy ước dedup dùng chung.
- **Tạo backlog: "gửi email xác thực tài khoản" (SRS FR-AUTH-01, ưu tiên Cao)** —
  phần này đã bị cắt khỏi PBL6-45 nên đang không có ticket nào nhận.
- Chạy Flyway thật + `ddl-auto: validate` (Testcontainers) → PBL6-47.
- **Luồng kích hoạt tài khoản (gửi OTP/link + endpoint verify-email): HOÃN** — chốt
  không làm trong sprint này. `email_verified` chỉ là cờ, kích hoạt để làm sau
  (không gắn vào PBL6-45; PBL6-45 chỉ lo forgot-password).
- PBL6-43 (login): user `PENDING` có được đăng nhập không? (cờ hiện chưa chặn gì)
- Chưa có endpoint GET user → response đăng ký không kèm `Location` header.
