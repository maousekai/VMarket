# Worklog — PBL6-15: Product Catalog Service

- **Branch:** `PBL6-15-product-catalog-service`
- **Service:** `services/product-service`
- **CSDL:** MongoDB
- **Phạm vi:** FR-PROD-01 đến FR-PROD-06 theo `docs/SRS-VMarket.md`
- **Ngày cập nhật:** 15/09/2026
- **Quy ước review:** mỗi đợt review được ghi theo mẫu `Review N`; các lần review
  sau tiếp tục tăng tuần tự `N` và bổ sung thành một phần mới, không ghi đè
  finding của các lần trước.

---

## 1. Công việc đã triển khai

- Xây dựng model MongoDB cho Product, ProductVariant, Category, Brand và
  InventoryReservation; bổ sung index và optimistic version cho Product.
- Xây dựng CRUD sản phẩm và biến thể cho Seller, giới hạn tối đa 9 ảnh, kiểm tra
  giá/tồn kho/SKU, ba trạng thái `DRAFT`, `ACTIVE`, `HIDDEN` và soft-delete.
- Xây dựng API duyệt, tìm kiếm cơ bản, lọc theo danh mục/giá/đánh giá/gian hàng,
  sắp xếp và phân trang; API chi tiết trả biến thể, tồn kho và sản phẩm tương tự.
- Xây dựng quản lý danh mục đa cấp, thứ tự hiển thị, trạng thái ẩn/hiện; CRUD
  thương hiệu và các API quản trị.
- Xây dựng kiểm duyệt sản phẩm: Admin gỡ/khôi phục kèm lý do.
- Xây dựng quy trình tồn kho theo `orderId`: giữ kho, xác nhận trừ kho và hoàn
  lượng đã giữ; thêm khóa gọi API nội bộ.
- Phát các event `ProductCreated`, `ProductUpdated`, `ProductDeleted`,
  `StockReserved`, `StockReleased` qua shared event bus.
- Bổ sung validation, error response dùng chung, kiểm tra role Seller/Admin,
  Swagger/OpenAPI và unit test cho các luồng nghiệp vụ chính.
- Xóa endpoint demo `ProductCreated`, thay bằng luồng phát event từ CRUD thật.

## 2. Kết quả kiểm chứng hiện tại

- `mvnw -pl product-service -am verify`: BUILD SUCCESS.
- 18 test của Product Service và 9 test của shared-events đều pass.
- CRUD, validation, soft-delete và routing key của các event đã có unit test.

## 3. Review 1 — Findings tại thời điểm tạo snapshot

1. **P1 — Shop ownership:** `shopId` trong request chưa được xác minh thuộc Seller
   và Shop đang ở trạng thái hoạt động.
2. **P1 — Event integration tồn kho:** chưa có consumer `OrderPlaced`; chưa có
   luồng tự động xác nhận/hoàn kho từ Order/Payment, hiện mới có REST API nội bộ.
3. **P1 — Atomicity tồn kho:** `synchronized` chỉ bảo vệ một JVM; cập nhật nhiều
   Product rồi mới lưu reservation có thể double-reserve hoặc ghi một phần khi
   chạy nhiều instance/lỗi giữa chừng.
4. **P1 — Atomicity MongoDB/RabbitMQ:** Product được lưu trước khi publish event;
   RabbitMQ lỗi có thể làm mất đồng bộ với Search/Recommendation.
5. **P2 — Event schema:** payload `ProductCreated`/`ProductUpdated` vẫn là schema
   demo tối thiểu, thiếu category/brand/description/variants/rating/tồn kho.
6. **P2 — Moderation notification:** gỡ/khôi phục chưa phát event riêng để
   Notification Service thông báo Seller.
7. **P2 — Category visibility:** con active của cha bị ẩn có thể bị nâng thành
   root; lọc descendant vẫn có thể lấy nhánh category đã ẩn.
8. **P2 — Price sort:** `PRICE_DESC` sort trực tiếp trên mảng `variants.price`,
   không bảo đảm nhất quán với `minPrice` hiển thị.
9. **P2 — Test quality:** chưa có test repository/controller/event integration,
   test concurrency/failure path và chưa có báo cáo coverage JaCoCo.

## 4. Hướng xử lý tiếp theo

- Bổ sung projection trạng thái/quyền sở hữu Shop từ event của Shop Service để
  Product Service xác minh cục bộ, không truy cập CSDL service khác.
- Hoàn thiện event contract và consumer cho luồng đặt hàng/tồn kho; thiết kế lại
  ghi tồn kho theo atomic operation, có idempotency bền vững.
- Dùng outbox cho Product/Stock event và bổ sung worker retry.
- Chuẩn hóa snapshot event, moderation event, category visibility và trường giá
  dẫn xuất phục vụ sort/index.
- Bổ sung integration/concurrency tests và JaCoCo gate.

## 5. Kết quả xử lý findings của Review 1

- Đã xử lý finding 1 bằng projection `ShopCatalogAccess` nhận
  `ShopApproved`/`ShopSuspended`; create/update kiểm tra đúng chủ Shop và Shop
  đang hoạt động. Khi đình chỉ, sản phẩm được ẩn và đồng bộ `ProductUpdated`.
- Đã xử lý finding 2 bằng consumer cho `OrderPlaced`, `PaymentSucceeded`,
  `OrderConfirmed` (COD) và `OrderCancelled`; bổ sung phản hồi
  `StockReservationFailed` khi không đủ kho.
- Đã xử lý finding 3 bằng Mongo transaction, optimistic version trên Product và
  InventoryReservation, cùng unique `orderId`; bỏ khóa `synchronized` cục bộ.
  Docker Compose khởi tạo MongoDB single-node replica set để hỗ trợ transaction.
- Đã xử lý finding 4 bằng transactional outbox. Event RabbitMQ được retry với
  cùng `eventId`; phía consumer retry 2 lần rồi chuyển vào DLQ.
- Đã xử lý finding 5–8: snapshot sản phẩm schema v2 đầy đủ, event
  `ProductModerated`, chặn nhánh danh mục ẩn và sort theo `minPrice` dẫn xuất.
- Đã bổ sung test cho query, event consumer, Shop projection, outbox failure và
  category visibility; JaCoCo gate tầng service đặt ở 60%.

## 6. Kiểm chứng sau khi xử lý Review 1

- `mvnw -pl product-service -am verify`: BUILD SUCCESS.
- Shared Events: 9 test pass; Product Service: 34 test pass.
- Line coverage tầng service: 350/471 dòng, xấp xỉ 74,3% (gate yêu cầu 60%).
- `docker compose config --quiet`: hợp lệ.
- `git diff --check`: không có whitespace error.

### Giới hạn kiểm chứng còn lại

- Chưa chạy integration/concurrency test trên Mongo replica set và RabbitMQ thật
  trong lượt này; transaction/outbox/DLQ đã được cấu hình và bao phủ bằng unit/
  context test, nhưng vẫn cần smoke test hạ tầng trước khi mở PR.

## 7. Review 2 — Findings còn tồn tại

Kết quả dưới đây được ghi nhận sau khi review lại snapshot `777db72` và toàn bộ
phần sửa sau Review 1, hiện đã được lưu trong snapshot `a679e8c`. `verify` đang
xanh nhưng chưa chứng minh được các luồng liên-service và failure mode bên dưới.

1. **P1 — Rating chưa bao giờ được đồng bộ:** SRS quy định Product nhận
   `ReviewCreated`, nhưng Product Service không có payload/consumer/binding cho
   event này. `ratingAverage` và `ratingCount` chỉ được khởi tạo bằng 0 khi tạo
   sản phẩm, nên lọc theo đánh giá và dữ liệu trang chi tiết sẽ luôn sai sau khi
   có review thật.
2. **P1 — Projection Shop không có bootstrap/reconciliation:** quyền tạo/sửa và
   việc ẩn sản phẩm phụ thuộc hoàn toàn vào một lần nhận `ShopApproved` hoặc
   `ShopSuspended`. Event phát trước khi `product.events` được khai báo, dữ liệu
   Shop đã tồn tại trước deployment, hoặc message bị mất đều không có cơ chế tự
   sửa; Seller hợp lệ có thể bị `SHOP_NOT_READY`, còn Shop bị đình chỉ có thể vẫn
   bán hàng.
3. **P1 — Luồng tồn kho chưa chạy end-to-end trong repo:** các consumer mới dùng
   `OrderConfirmed` và `OrderCancelled`, nhưng hai event này không nằm trong ma
   trận event hiện tại của SRS và không có producer nào trong Order Service.
   `ShopApproved`/`PaymentSucceeded` cũng chưa có producer thực tế vì Shop,
   Order và Payment Service hiện mới là skeleton health; do đó giữ/chốt/hoàn kho
   tự động chưa thể hoạt động ở cấp hệ thống.
4. **P1 — Outbox có thể đánh dấu đã phát khi broker chưa lưu event:**
   `OutboxDispatcher` gọi `RabbitTemplate.send()` rồi đặt `publishedAt` ngay,
   nhưng RabbitMQ chưa bật publisher confirms/returns và không chờ broker ACK.
   Mất kết nối hoặc broker lỗi đúng cửa sổ này vẫn có thể làm mất event, nên
   finding atomicity Mongo/Rabbit chưa được đóng hoàn toàn.
5. **P1 — Ẩn Category chưa vô hiệu hóa sản phẩm nhất quán:** `setVisibility()`
   chỉ lưu Category, không cập nhật/phát `ProductUpdated` cho sản phẩm thuộc
   category và descendants. Public catalog nội bộ đã chặn các sản phẩm này,
   nhưng Search index có thể tiếp tục hiển thị; `InventoryService` cũng chỉ kiểm
   tra trạng thái Product nên vẫn có thể giữ kho từ cart/order cũ.
6. **P2 — Workflow kiểm duyệt chặn Seller chỉnh sửa:** `update()` từ chối mọi
   Product có `moderationRemoved=true`. Điều này mâu thuẫn UC quản lý sản phẩm
   trong SRS, nơi sản phẩm bị gỡ cần được chỉnh sửa trước khi gửi lại; hiện không
   tồn tại trạng thái/API resubmit.
7. **P2 — Khôi phục trạng thái sai khi moderation xen kẽ shop suspension:** nếu
   Shop bị đình chỉ khi Product đang `ACTIVE`, sau đó Admin gỡ rồi khôi phục
   Product trước khi Shop được mở lại, `statusBeforeShopSuspension=ACTIVE` bị ghi
   đè bằng `HIDDEN`. Khi `ShopApproved` đến, Product vẫn `HIDDEN` thay vì trở lại
   trạng thái trước đình chỉ.
8. **P2 — Thiếu migration/backfill cho derived fields:** query mới sort theo
   `minPrice`, trong khi các document tạo bởi snapshot cũ không có `minPrice`,
   `maxPrice`, `availableStock`. Mapper có fallback cho hiển thị nhưng query và
   event factory không có fallback/backfill tương ứng, nên dữ liệu cũ sort sai
   hoặc phát snapshot giá/tồn kho `null`/0.
9. **P2 — Event input bỏ qua Bean Validation:** `OrderPlacedInventoryConsumer`
   tự tạo `InventoryRequest` rồi gọi service trực tiếp. Payload `items=null`,
   quantity ≤ 0, chuỗi rỗng hoặc phép cộng quantity overflow không được kiểm tra
   như REST API; dữ liệu xấu có thể tạo reserved stock âm hoặc bị retry/DLQ bằng
   lỗi kỹ thuật thay vì trả `StockReservationFailed` hợp lệ.
10. **P2 — Thay đổi arguments của durable queue thiếu kế hoạch migration:**
    shared-events hiện redeclare queue cũ với dead-letter arguments. RabbitMQ sẽ
    báo `PRECONDITION_FAILED` nếu queue cùng tên đã tồn tại mà chưa có các
    arguments này; rollout có thể làm toàn bộ consumer dùng thư viện chung không
    khởi động được nếu không version queue hoặc có bước migrate/delete rõ ràng.
11. **P2 — Outbox worker chưa an toàn khi scale nhiều instance:** mỗi instance
    cùng đọc top 50 pending event mà không có atomic claim/lease. Cùng một event
    có thể được gửi đồng thời nhiều lần; `eventId` ổn định chỉ giúp nếu tất cả
    downstream consumer đã triển khai deduplication, điều hiện chưa được chứng
    minh trong repo.
12. **P2 — Chưa có integration/concurrency test với hạ tầng thật:** test hiện
    mock repository/RabbitTemplate; context test dùng Mongo standalone và tắt
    listener/outbox. Vì vậy chưa kiểm chứng transaction nhiều Product, unique
    `orderId` dưới race, rollback domain + outbox, publisher confirm/DLQ và
    lifecycle replica set thực tế.
13. **P2 — Chưa xử lý `ReturnResolved`:** phụ lục event của SRS ghi Product là
    consumer của `ReturnRequested/ReturnResolved`, nhưng Product Service không
    có binding/consumer để cộng lại tồn kho cho các mặt hàng trả thành công.
14. **P3 — Các thao tác cây/Shop chưa có giới hạn tải:** mỗi request browse tải
    toàn bộ Category để tạo `$in`, còn suspend/approve Shop tải và cập nhật toàn
    bộ Product trong một Mongo transaction. Với catalog/shop lớn, transaction có
    thể vượt timeout và truy vấn browse khó đáp ứng mục tiêu latency; cần cache/
    materialized visibility và xử lý bulk theo batch có checkpoint.

## 8. Kết quả xử lý findings của Review 2

1. Đã bổ sung contract, binding và consumer `ReviewCreated`; Product lưu aggregate
   `ratingAverage`/`ratingCount` theo timestamp nguồn và phát `ProductUpdated` để
   đồng bộ Search/Recommendation.
2. Đã bổ sung endpoint nội bộ có xác thực để Shop Service đẩy snapshot bootstrap/
   reconciliation theo trang tối đa 500 Shop; projection lưu `updatedAt`, bỏ qua
   snapshot cũ và ghi nhận `reconciledAt`.
3. Đã bỏ contract/consumer tự đặt `OrderConfirmed` và `OrderCancelled`, thay bằng
   `OrderStatusChanged` đúng danh mục event SRS; trạng thái hủy hoàn giữ kho, COD
   được chốt ở `CONFIRMED`/`PREPARING`, còn PayOS tiếp tục chốt qua
   `PaymentSucceeded`.
4. Outbox đã bật publisher confirm/return, gửi mandatory, chờ broker ACK và kiểm
   tra returned message trước khi ghi `publishedAt`; NACK, timeout hoặc không có
   routing đều giữ event pending để retry.
5. Đã materialize `categoryPath` và `categoryVisible` lên Product. Thay đổi
   visibility cập nhật theo batch và phát `ProductUpdated`; browse, detail và giữ
   kho đều chặn nhánh danh mục bị ẩn.
6. Seller có thể chỉnh sửa Product đã bị moderation gỡ; Product vẫn `HIDDEN` cho
   đến khi Admin khôi phục. Đã thêm API resubmit và event
   `ProductModerationRequested`.
7. Đã tách trạng thái mong muốn trước moderation và trước Shop suspension, xử lý
   đúng thứ tự xen kẽ giữa gỡ/khôi phục Product và đình chỉ/mở lại Shop.
8. Đã thêm migration/reconciliation lúc startup và theo lịch để backfill
   `minPrice`, `maxPrice`, `availableStock`, `categoryPath`,
   `categoryVisible`; event factory có fallback tương thích document cũ.
9. Luồng event tồn kho dùng chung Bean Validation với REST; payload null/rỗng,
   quantity không dương và phép cộng overflow được chuyển thành lỗi domain hợp
   lệ thay vì làm hỏng dữ liệu.
10. Queue Product được nâng thành `product.events.v2`; tài liệu event bus quy định
    bắt buộc bump version khi đổi arguments của durable queue và chỉ xóa queue cũ
    sau khi drain/rollout hoàn tất.
11. Outbox worker dùng atomic claim/lease theo instance; chỉ worker đang giữ lease
    mới có thể đánh dấu publish hoặc cập nhật retry.
12. Đã thêm Testcontainers integration test trên MongoDB replica set và RabbitMQ
    thật, bao phủ rollback domain + outbox, race cùng/khác `orderId`, chống
    oversell/orphan reservation, broker ACK, unroutable retry và retry-to-DLQ.
13. Đã bổ sung `ReturnResolved`; chỉ nhập kho khi `restock=true`, kiểm tra đơn đã
    chốt, giới hạn tổng số lượng trả và dùng unique `returnId` để idempotent.
14. Browse theo danh mục dùng `categoryPath` thay vì tải toàn cây. Đồng bộ Category
    và Shop đọc/ghi Product theo cursor batch 100; mỗi batch có transaction +
    outbox riêng, không giữ transaction cho toàn bộ catalog/shop.

### Kiểm chứng sau khi xử lý Review 2

- `mvnw -pl product-service -am verify`: BUILD SUCCESS.
- `mvnw test` toàn bộ reactor 13 module: BUILD SUCCESS.
- Shared Events: 9 test pass; Product Service: 59 test pass, không có test bị skip.
- Integration suite: 6 test pass trên MongoDB replica set và RabbitMQ thật.
- Line coverage tầng service: 577/691 dòng, xấp xỉ 83,5% (gate yêu cầu 60%).
- `docker compose config --quiet`: hợp lệ.
- `git diff --check`: không có whitespace error.
- Cấu hình MongoDB đã dùng namespace Spring Boot 4 `spring.mongodb.uri`; thuộc
  tính `spring.data.mongodb.auto-index-creation` được giữ đúng namespace riêng.

### Phụ thuộc liên-service còn lại

- Product Service đã có đầy đủ contract, consumer và endpoint reconciliation ở
  phía nhận. Việc phát thật `ShopApproved`/`ShopSuspended`, `OrderPlaced`/
  `OrderStatusChanged`/`ReturnResolved`, `PaymentSucceeded` và `ReviewCreated`
  vẫn phải được hiện thực trong các ticket sở hữu Shop, Order, Payment và Review;
  bốn service này hiện chỉ có health skeleton. Vì vậy chưa thể tuyên bố luồng E2E
  toàn hệ thống đã chạy chỉ bằng thay đổi trong phạm vi PBL6-15.

## 9. Review 3 — Review tại workspace, kiểm chứng bằng Docker Compose thật

Review được thực hiện ngày 15/09/2026 trên working tree của commit `003cc02`
(sau snapshot `a679e8c`). Lần này không chỉ đọc code mà chạy trực tiếp hạ tầng
Docker Compose và smoke test service thật, gồm: `docker compose config`,
`docker compose up -d`, kiểm tra Mongo replica set, RabbitMQ queue/binding,
chạy `mvnw test` cho product-service và toàn bộ reactor 13 module, rồi khởi
động product-service nối vào Mongo/RabbitMQ thật để gọi API và soi outbox.

### 9.1. Kết quả kiểm chứng chạy thực tế

- `docker compose config --quiet`: hợp lệ (exit 0).
- `docker compose up -d`: 7/7 container healthy — `vmarket-postgres` (5433),
  `vmarket-mongo` (27018), `vmarket-redis`, `vmarket-rabbitmq`,
  `vmarket-gateway` (8080), `vmarket-auth-service` (8081), `vmarket-frontend`
  (5173). Không xảy ra xung đột port hay lỗi khởi tạo.
- Mongo chạy đúng single-node replica set: `rs.status().ok = 1`, 1 member —
  điều kiện cần cho Mongo transaction đã cấu hình.
- `mvnw -pl product-service -am test`: BUILD SUCCESS — shared-events 9 test,
  product-service 59 test, 0 fail / 0 skip.
- `mvnw test` toàn reactor 13 module: BUILD SUCCESS (mỗi service skeleton 2
  test, product-service 59, shared-events 9).
- Smoke test product-service thật (chạy ngoài container, nối `localhost:27018`
  và `localhost:5672`):
  - `/actuator/health` → UP; service khởi động sạch, nối replica set và
    RabbitMQ thành công, không warning cấu hình.
  - POST `/api/products/internal/shop-access/reconcile` (kèm
    `X-Internal-Api-Key`) → 204; dữ liệu persist vào MongoDB thật, snapshot
    Shop truy vấn lại đúng.
  - GET `/api/products` (browse public) → 200.
  - POST `/api/products/categories` bằng role ADMIN → 201, có slug.
  - POST `/api/products` bằng role SELLER sở hữu shop đã reconcile → 201;
    response trả đầy đủ trường dẫn xuất `minPrice`/`maxPrice`/
    `availableStock` đúng với biến thể.
  - Lỗi nghiệp vụ trả envelope có cấu trúc: 404 `CATEGORY_NOT_FOUND` khi
    tạo sản phẩm trỏ category chưa tồn tại.
- Queue RabbitMQ thực tế sau khi service khai báo: `product.events.v2` +
  `product.events.v2.dead` với đúng arguments DLX (không gặp
  `PRECONDITION_FAILED`), binding cho 7 event tiêu thụ (OrderPlaced,
  OrderStatusChanged, PaymentSucceeded, ShopApproved, ShopSuspended,
  ReviewCreated, ReturnResolved).

### 9.2. Findings mới

1. **P1 — Event catalog hiện không tới được consumer nào (xác minh trực
   tiếp trên broker):** sau khi tạo sản phẩm thật, outbox ghi nhận
   `ProductCreated` nhưng KHÔNG BAO GIỜ được publish — document trong
   `catalog_outbox` giữ `attempts` tăng dần kèm `lastError` dạng
   "Event không được route tới queue nào: NO_ROUTE". Nguyên nhân: broker
   không có queue nào bind routing key
   `ProductCreated`/`ProductUpdated`/`ProductDeleted` — consumer dự kiến là
   AI Search/Recommendation (Python) mới chỉ có skeleton, chưa khai báo
   queue/binding durable. Hệ quả: (a) tiêu chí DoD "phát đúng sự kiện
   ProductCreated/Updated/Deleted" mới đúng "tới tận outbox", chưa đúng ở
   mức hệ thống; (b) outbox retry vô hạn vì không có cơ chế max-attempts →
   park/dead, và không có alert/metric cho event kẹt; (c) integration test
   không phát hiện được vì test tự khai báo queue tạm + binding
   `ProductDeleted` trước khi enqueue, che mất trạng thái thiếu consumer của
   môi trường thật. Event không mất (nằm bền trong outbox, sẽ được phát khi
   consumer đăng ký queue), nhưng cần quyết định rõ ràng: khai báo sẵn queue
   cho AI Search/Recommendation trong repo, hoặc thêm max-retry/DLQ + alert
   cho outbox, hoặc ghi nhận rõ trạng thái này trong tài liệu event bus.
2. **P2 — Endpoint nội bộ vẫn đi qua public gateway:** gateway route
   `Path=/api/products/**` không chặn `/api/products/internal/**`, nên mọi
   request POST nội bộ (reconcile shop-access) đều đi qua cửa công khai; lớp
   bảo vệ duy nhất là `X-Internal-Api-Key` tĩnh chia sẻ qua env. Cần thêm
   rule deny `/api/products/internal/**` ở gateway (hoặc cổng quản trị riêng)
   như một lớp defense-in-depth, phòng khi key bị lộ trong log/env sai.
3. **P2 — Outbox thiếu cơ chế chết hẳn cho event lỗi vĩnh viễn:** event
   unroutable hoặc payload lỗi serialize sẽ retry mãi với backoff tối đa 60s,
   không có trạng thái DEAD/parking-lot riêng, không có metric đếm
   pending/failed để cảnh báo. Kết hợp finding 1, đây là điểm cần chốt trước
   khi mở PR.
4. **P3 — Hợp đồng lỗi chưa nhất quán cho lỗi Bean Validation:** lỗi nghiệp
   vụ domain trả envelope có `error` là mã nghiệp vụ (vd
   `CATEGORY_NOT_FOUND`), trong khi lỗi Bean Validation ở controller trả
   envelope mặc định Spring (400, không có mã lỗi chi tiết) — quan sát trực
   tiếp khi POST category thiếu trường. Nên chuẩn hóa qua `@ControllerAdvice`
   bắt `MethodArgumentNotValidException`.
5. **P3 — Thứ tự message chưa được bảo vệ:** `OrderStatusChanged(CANCELLED)`
   tới trước khi reservation `OrderPlaced` được xử lý (out-of-order) sẽ gây
   `RESERVATION_NOT_FOUND` → retry 2 lần → DLQ, và hoàn kho bị mất nếu
   không re-drive thủ công. Rủi ro thấp nhưng nên ghi nhận trong tài liệu
   vận hành DLQ hoặc bỏ qua có chủ đích trạng thái "chưa có reservation".

### 9.3. Đối chiếu kết quả xử lý Review 2 (xác minh ở mức code + chạy thật)

- Finding 1 (ReviewCreated): có contract, binding và consumer; guard theo
  timestamp nguồn trong `ProductRatingService.synchronize` — đạt.
- Finding 2 (Shop bootstrap/reconciliation): endpoint nội bộ hoạt động thật
  (204, persist, guard thời gian, giới hạn 500 shop/request) — đạt.
- Finding 3 (OrderStatusChanged thay cho OrderConfirmed/OrderCancelled):
  contract mới tồn tại, consumer ánh xạ COD (`CONFIRMED`/`PREPARING`) và hủy,
  PayOS chốt qua `PaymentSucceeded` — đạt ở phía nhận.
- Finding 4 (publisher confirm/mandatory/returned): code đúng và cơ chế
  returned-check được chứng minh hoạt động thật (chính nó bắt được NO_ROUTE ở
  finding 1 của Review 3) — đạt.
- Finding 5 (categoryPath/categoryVisible): product tạo mới được materialize
  đúng, browse/detail/giữ kho chặn nhánh ẩn — đạt.
- Finding 6/7 (moderation + shop suspension xen kẽ): tách
  `statusBeforeModeration`/`statusBeforeShopSuspension`, API resubmit và event
  `ProductModerationRequested` đầy đủ ở mức code — đạt.
- Finding 8 (migration/backfill): chạy lúc khởi động + reconcile mỗi giờ; sản
  phẩm tạo thật có đủ trường dẫn xuất — đạt.
- Finding 9 (Bean Validation cho event input): `InventoryInputValidator` dùng
  chung, normalize gộp item trùng và `Math.addExact` chống tràn số — đạt.
- Finding 10 (queue version): `product.events.v2` khai báo sạch trên broker
  thật, kèm quy định bump version trong `docs/event-bus.md` — đạt.
- Finding 11 (outbox claim/lease): atomic `findAndModify` với
  `claimedBy`/`claimedUntil`, mark Published/Failed đều kiểm tra người giữ
  lease — đạt.
- Finding 12 (integration test): 6 test Testcontainers chạy trên MongoDB
  replica set + RabbitMQ thật — đạt, nhưng lưu ý khoảng trống "không có
  consumer ngoài" nêu ở Review 3 finding 1.
- Finding 13 (ReturnResolved): `restockReturn` chỉ nhập khi `restock=true`,
  chặn đơn chưa chốt, giới hạn số lượng trả theo lịch sử `ReturnRestock`,
  idempotent theo `returnId` — đạt.
- Finding 14 (batch giới hạn tải): cursor batch 100 cho Category/Shop, mỗi
  batch một transaction + outbox riêng — đạt.

### 9.4. Kết luận về DoD

- CRUD sản phẩm/biến thể hoạt động đúng: **đạt** — đã xác minh bằng API thật
  trên hạ tầng Docker Compose.
- Phát đúng sự kiện ProductCreated/Updated/Deleted: **đạt một phần** — event
  được ghi outbox bền vững và có cơ chế xác nhận broker, nhưng hiện chưa có
  consumer nào trên bus (P1 Review 3) nên chưa thể coi là "phát đúng" ở mức
  hệ thống cho đến khi AI Search/Recommendation khai báo queue hoặc có phương
  án xử lý event kẹt.
- PR review & merge vào `dev`: **chưa thực hiện** — cần xử lý/định hướng xong
  P1 trước khi mở PR.

### 9.5. Hướng xử lý đề xuất sau Review 3

- Khai báo queue/binding durable cho consumer catalog (AI Search,
  Recommendation) ngay trong repo, hoặc bổ sung cấu hình max-attempts → DEAD
  kèm metric cho outbox; cập nhật `docs/event-bus.md` ghi rõ trạng thái "chưa
  có consumer" và cách vận hành.
- Thêm rule gateway chặn `/api/products/internal/**` và đưa vào bộ kiểm thử
  gateway.
- Chuẩn hóa error envelope cho lỗi Bean Validation bằng `@ControllerAdvice`
  chung.
- Ghi chú quy ước xử lý out-of-order cho consumer tồn kho (bỏ qua có kiểm
  soát hoặc requeue có hạn thay vì DLQ im lặng).

## 10. Xử lý các finding của Review 3 (phương án song song)

Ngày 15/09/2026. Thực hiện đồng thời cả hai hướng đã đề xuất ở §9.5 thay vì
chọn một, kèm xử lý nốt 3 finding còn lại (F2, F4, F5). Tất cả thay đổi code
trong phạm vi ticket PBL6-15; phần khai báo queue Python và cập nhật
`docs/event-bus.md` phục vụ luôn cho ticket AI (PBL6-39).

### 10.1. F1 — Outbox max-attempts → DEAD + metrics (P1)

- `OutboxEvent` thêm trường `deadAt`/`deadReason`; `OutboxEventStore` thêm
  `markDead`, `countPending`, `countDead`; claim chỉ lấy event chưa DEAD và
  chưa tới lượt retry.
- `OutboxDispatcher` thêm `app.outbox.max-attempts` (mặc định 10): khi cạn
  lượt thử → `markDead` + tăng metric `vmarket.outbox.dead`; mỗi lần retry
  có lease tăng `vmarket.outbox.retry`. Event unroutable không còn retry vô
  hạn mà bị park tạm, cạn lượt thì DEAD.
- Micrometer: gauge `vmarket.outbox.pending`, `vmarket.outbox.dead.total`;
  actuator expose thêm `metrics,prometheus`.
- Dọn broker thật: bỏ 3 binding ProductCreated/Updated/Deleted khỏi
  `product.events.v2` (product-service không được tự nhận event catalog của
  chính nó — đây mới là nguyên nhân NO_ROUTE trước đây, không phải do thiếu
  queue); 7 binding event tiêu thụ (Order/Payment/Shop/Review/Return) giữ
  nguyên. Event unroutable không rơi DLQ broker mà được park rồi DEAD trong
  `catalog_outbox` (Mongo) theo cơ chế F1, quan sát qua metric outbox.
- **E2E đã chứng minh trên hạ tầng thật:** khởi động product-service (code
  mới) + chạy `event_consumer.py` của cả 2 service AI (pika) → tạo sản phẩm
  qua API → event `ProductCreated` được outbox publish tới cả 2 queue
  `ai-search.events` và `recommendation.events`, không còn NO_ROUTE, outbox
  trống, không event nào rơi DLQ.
- Test: `OutboxDispatcherTest` thêm test DEAD; tổng product-service giờ 63
  test (59 cũ + 4 mới).

### 10.2. F2 — Gateway chặn đường internal (P2)

- `JwtAuthenticationFilter` thêm bước deny `/api/products/internal/**`: mọi
  request đi qua gateway vào path nội bộ bị trả 403
  `INTERNAL_ENDPOINT_FORBIDDEN` ngay tại gateway; chỉ internal-caller
  (service-to-service trong network, không qua gateway) gọi trực tiếp
  product-service được.
- `GatewaySecurityProperties` thêm property `internal-deny-paths` (mặc định
  rỗng), cấu hình trong `application.yml` gateway.
- Test gateway giờ 26 test (12 test filter, trong đó 2 test mới: SELLER có
  JWT hợp lệ vẫn bị 403 vào internal path; no-token bị 403 trước khi tới
  bước JWT).

### 10.3. F3 — Queue Python + tài liệu event bus (P1)

- `ai-search-service/event_consumer.py`: sửa `queue_declare` có `arguments`
  DLX (`x-dead-letter-exchange: vmarket.events.dlx`,
  `x-dead-letter-routing-key: ai-search.events.dead`) + khai báo thêm DLQ
  `ai-search.events.dead` (durable, bind `vmarket.events.dlx`).
- `recommendation-service`: tạo mới `event_consumer.py` (trước đây chỉ có
  main.py, không có consumer) — queue `recommendation.events` + DLQ
  `recommendation.events.dead`, bind `ProductCreated`/`Updated`/`Deleted`,
  `prefetch_count=1`, daemon thread tự reconnect; `main.py` gọi
  `start_event_consumer_thread()` trong lifespan; `requirements.txt` thêm
  `pika`.
- `docs/event-bus.md`: thêm §8 "Cấu hình RabbitMQ cho service Python" (mẫu
  pika đầy đủ: DLX arguments, DLQ, bind), cập nhật §7 DLQ + outbox (quy ước
  max-attempts, park, DEAD, re-drive thủ công, 4 metric outbox), mục "Service
  nhận event" bổ sung recommendation-service.

### 10.4. F4 — Chuẩn hóa error envelope (P3)

- `GlobalExceptionHandler` bổ sung handler cho `HttpMessageNotReadableException`

  (body không phải JSON), `MethodArgumentTypeMismatchException`,
  `NoHandlerFoundException`/`NoResourceFoundException` (404),
  `HttpRequestMethodNotSupportedException` (405) — tất cả trả envelope thống
  nhất `{timestamp,status,error,message,path}` với mã lỗi nghiệp vụ
  (`VALIDATION_FAILED`, `NOT_FOUND`, `METHOD_NOT_ALLOWED`), thay vì envelope
  mặc định/HTML của Spring.
- Xác minh bằng API thật: POST body lỗi → 400 `VALIDATION_FAILED`; GET path
  ảo → 404 `NOT_FOUND` envelope.

### 10.5. F5 — Out-of-order tồn kho (P3)

- `InventoryService.release/confirm/restockReturn`: khi event tới trước
  `OrderPlaced` (chưa có reservation, hoặc đơn hủy trước khi reservation được
  tạo) → bỏ qua có kiểm soát + metric
  `vmarket.outbox.out-of-order` (tag `action` = release/confirm/restockReturn)
  thay vì ném exception dẫn tới DLQ mất hoàn kho.
- Test: `InventoryServiceTest` thêm 2 test skip có kiểm soát
  (release-before-reserve, restock-before-reserve); `InventoryEventConsumerTest`
  xác nhận shape logic mới không phá luồng chính.

### 10.6. Kiểm chứng sau khi xử lý Review 3

- `mvnw -pl product-service test`: **63/63 PASS** (surefire:
  CatalogDataMigration 1, CatalogProjection 5, CategoryService 6, Health 1,
  InventoryEventConsumer 4, InventoryInputValidator 1, InventoryService 12,
  OutboxDispatcher 3, ProductCatalogQuery 2, ProductCatalogService 7,
  ProductController 3, ProductEventPublisher 1, ProductInfrastructure
  Integration 6, ProductRating 3, ProductServiceApplicationTests 2,
  RequestIdentity 2, ShopAccessService 4).
- `mvnw -pl api-gateway test`: **26/26 PASS** (ApiGateway 1,
  InMemoryRateLimiter 4, JwtAuthenticationFilter 12 — gồm 2 test F2 mới,
  JwtService 4, RateLimitFilter 5).
- Smoke test E2E trên hạ tầng thật: product-service (code mới) + cả 2
  consumer Python (pika) chạy đồng thời → tạo category + sản phẩm qua API
  (JWT dev, role SELLER sở hữu shop-1) → 201; event `ProductCreated` tới cả
  2 queue `ai-search.events` và `recommendation.events` (xác minh bằng log
  pika + `rabbitmqctl list_queues`), outbox trống sau khi flush, không event
  nào rơi DLQ.
- Env mới cho production: `OUTBOX_MAX_ATTEMPTS` (mặc định 10); metrics đọc
  tại `/actuator/metrics` và `/actuator/prometheus`.

### 10.7. Trạng thái các finding Review 3 sau xử lý

| Finding | Mức | Xử lý |
|---|---|---|
| F1 no-consumer → retry vô hạn | P1 | max-attempts → DEAD trong `catalog_outbox` + metrics + DLQ `.dead` cho từng queue consumer + E2E verified |
| F2 internal path qua gateway | P2 | deny list tại gateway (`internal-deny-paths`) + 2 test mới, 26/26 xanh |
| F3 docs + queue Python | P1 | `event_consumer.py` cho cả 2 service AI + §8 docs |
| F4 error envelope | P3 | 5 loại exception trả envelope chuẩn + verify API thật |
| F5 out-of-order tồn kho | P3 | skip có kiểm soát + metric `out-of-order` + 2 test mới |

