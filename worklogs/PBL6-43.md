# Worklog — PBL6-43: FR-AUTH-02 Login (JWT + lockout)

- **Story / subtask:** PBL6-12 Auth Service — PBL6-43 (FR-AUTH-02)
- **Branch:** `PBL6-43-login-jwt-lockout` (từ `dev` sau khi PBL6-42 merge — `6111510`)
- **Ngày:** 07/09/2026
- **Phạm vi:** `POST /api/auth/login`, `POST /api/auth/refresh`, khoá tài khoản sau
  5 lần sai, xoay vòng refresh token. **Không** làm: logout / revoke thủ công (PBL6-46),
  Google login (PBL6-44).

---

## 1. Quyết định thiết kế (đã xác nhận)

| # | Quyết định |
|---|---|
| Khoá tài khoản | Sai mật khẩu **5 lần liên tiếp** → khoá **15 phút**. Đăng nhập đúng reset bộ đếm. Khi khoá thì reset bộ đếm về 0, `locked_until` là cổng chặn |
| User PENDING | **Đăng nhập được**, response `status = PENDING` + claim `email_verified=false` trong JWT. FE hiển thị màn hình "chưa xác thực email", không cho dùng hệ thống bình thường. BE chưa chặn (siết quyền là PBL6-46) |
| Định danh login | **Chỉ email** |
| Refresh token | **Xoay vòng + phát hiện dùng lại**. TTL **15 ngày**. Dùng lại token đã `revoked_at` → thu hồi toàn bộ phiên user |
| Access token | JWT HS256, TTL 15 phút (SRS NFR-SEC-02 ≤ 30'). Claims: `sub`, `email`, `username`, `roles`, `email_verified`, `iss=auth-service` |
| Refresh token dạng | Chuỗi ngẫu nhiên 256-bit base64url (không phải JWT). DB chỉ lưu SHA-256 hex |
| Không enumeration ở login | Email lạ và sai mật khẩu đều trả `401 INVALID_CREDENTIALS` |

## 2. File

### Thêm
- `db/migration/V3__add_login_lockout_and_token_rotation.sql` — `users`:
  `failed_login_attempts INT NOT NULL DEFAULT 0`, `locked_until`;
  `refresh_tokens`: `revoked_at`, `replaced_by VARCHAR(26)` (audit, không FK).
  Không tạo partial index — index `idx_refresh_tokens_user_id` (từ V1) đã đủ.
- `security/JwtService.java` — sinh access token HS256, khoá từ `auth.jwt.secret`.
- `security/OpaqueTokenCodec.java` — sinh refresh token gốc + hàm SHA-256.
- `dto/LoginRequest.java`, `dto/RefreshRequest.java`, `dto/TokenResponse.java`.
- `service/AuthenticationService.java` — `login()` + `refresh()`.
- Test: `LoginApiTest` (6), `RefreshApiTest` (6), `JwtServiceTest` (2),
  `FlywayMigrationTest` (2 — chạy Flyway V1→V3 thật trên H2 + `ddl-auto: validate`).

### Sửa
- `entity/User.java` — field `failedLoginAttempts`, `lockedUntil`.
- `entity/RefreshToken.java` — field `revokedAt`, `replacedBy`.
- `config/AuthJwtProperties.java` — refresh TTL mặc định `30d → 15d`; `@Size(min=32)` cho `secret`.
- `application.yml` + `.env.example` + `docker-compose.yml` + `src/test/resources/application.yml`
  — `AUTH_JWT_REFRESH_TTL` default `15d`.
- `repository/RefreshTokenRepository.java` — `revokeIfActive` (guard `where revoked_at is null`,
  trả số dòng để bắt race), `revokeAllActiveByUserId`; bỏ `deleteByUserId` chưa dùng.
- `repository/UserRepository.java` — `incrementFailedLoginAttempts` / `lockUntil` / `clearLock`
  (UPDATE nguyên tử).
- `controller/AuthController.java` — thêm `/login`, `/refresh` + Swagger.
- `V1__init_auth_schema.sql` — comment kế hoạch migration ghi đúng số ticket
  (V2=PBL6-42, V3=PBL6-43); đổi `TIMESTAMPTZ` → `TIMESTAMP WITH TIME ZONE`
  (chuẩn SQL, chạy cả Postgres lẫn H2). V3 cũng đổi tương tự.
- `README.md` — bảng endpoint + mục "Đăng nhập" + roadmap.
- `entity/BaseEntity.java`, `V1__init_auth_schema.sql`, `worklogs/PBL6-41.md` —
  dọn tham chiếu file quy ước cục bộ còn sót từ PBL6-41 (commit `eaa072d`).
  **Lưu ý: file V1 bị sửa lại (comment + kiểu timestamp) → checksum Flyway đổi;
  ai đã chạy V1 ở DB local cần `./mvnw flyway:repair` hoặc drop `vmarket_auth`.**

## 3. Kiểm thử

- `./mvnw.cmd -pl auth-service -am test` — **BUILD SUCCESS, 31/31 pass** (H2).
  `LoginApiTest` (6): đăng nhập đúng/sai, đủ 5 lần → 423 (đã persist), khoá rồi
  mật khẩu đúng vẫn 423, email lạ → cùng code, PENDING → 200. `RefreshApiTest`
  (6): xoay vòng, dùng lại token cũ quá ân hạn → thu hồi cả chuỗi (đã persist),
  retry trong ân hạn → chỉ từ chối, **tài khoản bị khoá → thu hồi hết token +
  423**, token hết hạn, token lạ. `FlywayMigrationTest` (2): V1→V2→V3 chạy thật,
  `ddl-auto: validate` xác nhận entity khớp schema migration. `JwtServiceTest`
  (2): claims + chữ ký sai → `JwtException`.
- PostgreSQL thật end-to-end (Testcontainers) → PBL6-47.

## 4. Rà soát code — 9 điểm, xử lý hết

| # | Điểm | Xử lý |
|---|---|---|
| 1 🔴 | `login()` ghi bộ đếm sai rồi `throw ApiException` (RuntimeException) trong cùng `@Transactional` → Spring rollback → **khoá tài khoản không bao giờ có tác dụng ở prod** | `@Transactional(noRollbackFor = ApiException.class)` |
| 2 🔴 | `refresh()` gọi `revokeAllActiveByUserId()` rồi throw `REFRESH_TOKEN_REUSED` → rollback → **thu hồi phiên khi phát hiện đánh cắp là no-op ở prod** | như trên |
| 3 🟠 | `refresh()` không kiểm tra `locked_until` → user bị khoá vẫn lấy được access token mới | Thêm check `isLocked` trong `refresh()`; khi khoá thì thu hồi luôn toàn bộ refresh token (SRS FR-AUTH-06) |
| 4 🟠 | Retry mạng của `/refresh` (token đã xoay vòng) → `REFRESH_TOKEN_REUSED` → đăng xuất user trên mọi thiết bị | Khoảng ân hạn 30s: dùng lại token vừa xoay vòng → chỉ trả `401 INVALID_REFRESH_TOKEN`, không thu hồi chuỗi. Quá 30s mới coi là đánh cắp |
| 5 🟠 | Enumeration/timing: email lạ bỏ qua BCrypt (~1ms), email thật chạy BCrypt (~100ms) | Chạy BCrypt giả (`DUMMY_HASH`) cho nhánh email lạ. (423 khi bị khoá vẫn giữ — cần cho UX, giống quyết định enumeration ở đăng ký) |
| 6 🟠 | Test `@Transactional` che lỗi #1/#2 (rollback không thực sự xảy ra trong test) | `LoginApiTest` / `RefreshApiTest` bỏ `@Transactional`, dọn `@AfterEach`; assert trạng thái DB sau khi throw |
| 7 🟠 | `registerFailedAttempt` read-modify-write `failed_login_attempts`, không nguyên tử → 5 request song song mất tăng, né được khoá | UPDATE nguyên tử `set failed_login_attempts = failed_login_attempts + 1` trong repo |
| 8 🟡 | `RefreshToken.isActive()` là dead code | Bỏ |
| 9 🟡 | `secret` chỉ `@NotBlank`, javadoc nói ≥32 byte nhưng không ép → lỗi `WeakKeyException` khó hiểu ở prod | Thêm `@Size(min = 32)` với message chỉ đúng tên biến |

## 4b. Rà soát lượt 2 (trên PR #7)

| # | Điểm | Xử lý |
|---|---|---|
| F1 🔴 | `refresh()` khi tài khoản bị khoá chỉ throw 423, **không** thu hồi refresh token (sai với FR-AUTH-06, và với trường hợp Admin khoá tay) | Gọi `revokeAllActiveByUserId` trước khi throw `ACCOUNT_LOCKED`. Thêm test `refresh_whenAccountLocked_revokesAllTokens_and423` (mô phỏng Admin khoá tay) |
| F3 🔴 | Migration V3 chưa từng chạy thật (test dùng `flyway.enabled=false` + `create-drop`) | Thêm `FlywayMigrationTest`: `flyway.enabled=true` + `ddl-auto: validate` trên H2 (DB riêng) → V1→V2→V3 chạy thật, entity đối chiếu schema migration. Đổi `TIMESTAMPTZ` → `TIMESTAMP WITH TIME ZONE` để migration chạy được trên cả H2 lẫn Postgres. Testcontainers/Postgres thật → PBL6-47 |
| F2 🟡 | Comment kế hoạch trong `V1` ghi sai số ticket (V2=PBL6-43...) | Sửa: V2=PBL6-42, V3=PBL6-43 |
| F4 🟡 | `src/test/resources/application.yml` còn `refresh-ttl: 30d` | → `15d` |
| F5 🟡 | `JwtServiceTest` chữ ký sai chỉ assert "có ném exception" | Thêm `.isInstanceOf(JwtException.class)` |
| F9 🟡 | Số liệu test trong worklog mục 2 lệch (4 test / 27) | Sửa cho khớp thực tế |

Không sửa (chỉ thông tin): F6 timing oracle `/refresh` (token 256-bit ngẫu nhiên),
F7 lockout DoS (xử lý bằng rate-limit ở Gateway), F8 race nhỏ trong bộ đếm (idempotent),
F10 access token không có `jti` (→ PBL6-46).

Build sau tất cả fix: `./mvnw.cmd -pl auth-service -am test` — **BUILD SUCCESS, 31/31 pass** (H2).

## 5. TODO chuyển tiếp

- **Migration Flyway tiếp theo: V4** (V3 đã dùng).
- `AUTH_JWT_SECRET` dùng chung với API Gateway khi gateway verify JWT (PBL6-46).
- Logout / revoke refresh token thủ công, hỗ trợ nhiều thiết bị → PBL6-46.
- Idempotency-Key — vẫn chờ nhóm chốt quy ước dedup dùng chung.
- Chạy Flyway thật + `ddl-auto: validate` (Testcontainers) → PBL6-47.
